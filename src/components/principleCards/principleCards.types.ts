import { PrincipleCardProps } from '../principleCard/principleCard.types';

export interface PrincipleCardsProps {
  className?: string;
  cards: PrincipleCardProps[]; // Массив данных для карточек
}
